# Lumen 多需求接续（2026-06-15 · 污染中断，供新会话接手）

> 本会话 fast mode + dontAsk 长会话，工具输出**严重污染**（Edit 假成功、Read 双重渲染、
> grep 漏报、cargo 行号对不上）。新会话请先按「§0」核验真实状态，**别信本文档任何
> 「已落盘」结论**——以 `git diff` 与 `cargo check` 为唯一 ground truth。

## §0 新会话开场三步
1. **关掉 fast mode**（治污染根本，新通道即干净）。
2. 真实改动：`git -C F:/ClaudeWorkspaces/Lumen status --short` + `git diff`。
3. 真实编译错误：`cargo check -p lumen-app --tests 2>&1 | tail -50`，逐个修。

**当前已知唯一待修编译错**：`shell/mod.rs` 的 `tresp.context_menu(|ui| ...)` 报
`E0061 takes 0 arguments`（egui 0.34.3）。但侧栏 `resp.context_menu(|ui| ...)` 同写法不报错——
疑污染误报或借用问题，需干净环境复核（见需求2 修法）。

---

## §1 六个需求 + 状态

### 需求4 ghost 联想文本调暗 ✅ 应已落盘
`crates/lumen-renderer/src/lib.rs`（footer Composer 分支，约 1127-1215）：`dim_color/dim_attrs` 后新增
`ghost_color = glyphon::Color::rgba(fg.0,fg.1,fg.2,80)`（原 dim alpha 128）+ `ghost_attrs`；ghost 的
`set_text` 由 `&dim_attrs` 改 `&ghost_attrs`。placeholder 仍用 dim。

### 需求1 隐藏「运行中…（直通模式）」横条 ✅ 应已落盘（1-A 彻底隐藏）
- `composer.rs`：`InputMode::Running` 两处（feature + non-feature）由 `running(...)` 改 `hidden()`；
  non-feature 版删 `let s`、Running 并入 `AltScreen|Fallback`；顶部 `use crate::i18n;` 加 `#[cfg(feature="input-editor")]`。
  测试改：`running_模式_产出_hidden_形态`、`running_隐藏_高度为零_compose_有高度`、ghost 非 compose 断言 Hidden。
- `i18n/strings.rs`：`footer_running_text` 改无条件 `#[allow(dead_code)]`。
- 取舍：Compose↔Running 切换会 resize，海风哥接受（已注释）。

### 需求5 Ctrl+C 选区复制 ✅ 应已落盘
`keymap.rs` 层5（约 262-302）：`Running|AltScreen|Fallback` 三态统一「有选区→CopySelection、
有块→CopyBlock、否则 Interrupt」（复制后清选区，再按即中断）。改注释+match；测试删
`safety_ctrl_c_running_with_selection_is_interrupt`、`ctrl_c_alt_screen_with_selection_still_interrupt`，
加 running/alt_screen 各「with_selection→copy」「no_selection→interrupt」共 4 个。

### 需求3 外部文件拖拽插路径 ✅ 应已落盘
`main.rs`：抽 `AppState::insert_path_into_pane(&mut self, pi, path:&Path)`（focus_pane 之后约 2003，
逻辑=原 insert_path 的 Compose 分流/写 PTY，复用 `filetree::path_insert_text(_str)`）；原
`shell_out.insert_path`（约 5804）改调用它；新增 `WindowEvent::DroppedFile(path)` arm（CloseRequested
后），插入**焦点窗格**（winit 无 drop 坐标，平台限制）。

### 需求2 窗格自定义名（双击/右键重命名+持久化）🔧 代码全写，卡 context_menu 编译错
**逐处用 git diff 核验真落盘（污染期多次假成功，尤其 session.rs/sessions_store.rs 反复没落）**：
- `session.rs`：`Session` 加 `pub custom_title: Option<String>,`（initial_cwd 后）；`spawn` 的
  `Ok(Self{...})` 加 `custom_title: None,`。
- `sessions_store.rs`：`PaneEntry` 加 `pub custom_title: Option<String>,`（cwd 后，已有 serde default）。
- `main.rs`：snapshot（约 2493 PaneEntry）加 `custom_title: p.custom_title.clone()`；restore（约 2751）
  `Ok(mut s) => { s.custom_title = pane_entry.custom_title.clone(); ... }`；PaneView.title（约 4567）
  `p.custom_title.clone().filter(|s|!s.trim().is_empty())` 优先；`shell_out.pane_rename` 处理（紧跟
  tab rename 后约 5309）写 `panes[pi].custom_title` + persist；焦点 3 处（`update_terminal_focus`
  约 1969 加 `||pane_renaming.is_some()`；约 4589 加 `was_pane_renaming`；约 5261 焦点交还合并条件）。
- `shell/mod.rs`：`ShellState` 加 `pane_renaming:Option<(usize,String)>`+`pane_rename_focus:bool`；
  `ShellOutput` 加 `pane_rename:Option<(usize,String)>`+`pane_rename_ended_by_key:bool`；
  `draw_terminal_area`（有 `st:&mut ShellState`）窗格标题栏（约 907-949）`is_renaming` 分支画
  `TextEdit::singleline`（request_focus/lost_focus：Enter→`out.pane_rename`、记 ended_by_key、清状态），
  else 分支 Label + `double_clicked()` 进重命名 + `context_menu` 右键「重命名」+ 原拖动换位。
- i18n：复用 `s.menu_rename`，未新增。
- **待修**：`tresp.context_menu(|ui|{ ... ui.close(); })` E0061。修法：(a) 核 egui 0.34
  `Response::context_menu` 真签名并对齐侧栏 1458 写法（`ui.close()` 在 0.34 可能要 `ui.close_menu()`）；
  (b) 难定就**先删右键 context_menu 块、只留双击重命名**（双击是主入口），保证编译，右键后补。

### 需求6 窗口缩小再放大内容挤左侧窄列 🔍 调查完未修
既有 bug（旧 exe 复现）。Explore 假设（**未确诊**）：`main.rs:6071-6078` x0/x1 独立 round 致 tw 波动 +
`grid.rs:315` resize cache 跳过 + footer debounce（`history.rs:696-713`）。但「缩小到 N 列放大回 M 列、
N≠M 本会真 resize、不该卡」——假设站不住。**需真机复现+加日志精确定位再修，勿盲改 resize 核心**
（极敏感，进度文档「最大化还原每行剩首字符」即出此块）。

## §2 验证清单（全改完后）
1. `cargo check -p lumen-app --tests`（先修 context_menu）
2. `cargo clippy -p lumen-app`（0 warning）
3. `cargo test -p lumen-app`（含改过的 keymap/composer 测试）
4. 真机 `target/debug/lumen.exe` 验：ghost 更暗 / 运行命令无横条 / claude 里选中 Ctrl+C 复制 /
   拖文件进终端插路径 / 窗格双击+右键重命名且重启保留
5. 版本号 bump（等海风哥拍板，流程见 `docs/进度与待办-接续.md`）

## §3 污染经验（待写 engram）
dontAsk+fast mode 长会话叠加污染最毒：Edit/Bash 假成功；Read 双重渲染/旧内容/串台；grep 漏报；
printf→rint 输入侧串改。自保：cargo check 最硬 ground truth；git status/diff marker 单命令重跑取一致；
Edit「String not found」多半 old_string 中毒；engram write 回显 id≠落库（recall 核验）。根治：重启+关 fast mode。
