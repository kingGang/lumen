# Lumen 需求池（Backlog）

> 收集验收反馈与新需求，统一排期。状态：待办 / 进行中 / 完成。来源标注提出时间。

## 功能需求（海风哥 2026-06-11 提出）

| # | 需求 | 量级评估 | 归属建议 | 状态 |
|---|---|---|---|---|
| F1 | **自绘标题栏**：不用系统顶部标题栏，自己做——无边框窗口 + 标题栏与顶栏融合（tab/头像/窗口控制按钮一体，Warp/VSCode 形态）；需处理窗口拖动、双击最大化、Win11 Snap Layouts 悬停 | 中（1~2 天）：winit decorations=false + egui 拖动区 hit-test + 原生窗口按钮行为 | **M3.7**（打磨批次后、M4 前）——改动集中在外壳层，与 M3.5 顶栏直接融合 | ✅ 完成（M3.8：批1 `53c2648` 无边框（decorations=false + with_undecorated_shadow 保 DWM 阴影/Win11 圆角 + set_resizable(true) 绕 winit #4186）+ 顶栏窗控三按钮（关闭悬停红底 #c42b1c，文案 i18n wc_* 三语）+ 标题空白区拖动/双击最大化/右键系统菜单（五信号同帧执行，close 与既有退出同路落盘）；批2 `df33528` Snap Layouts——windows-sys SetWindowSubclass 子类化 WM_NCHITTEST 返 HTMAXBUTTON（winit #3884 无原生支持），按钮屏幕矩形 AtomicI32 每帧同步，NC 点击按 Microsoft 文档吞 DOWN/UP 内 ShowWindow 切换；评审 4 minor 全修 `3f37eab`（注释两处/WM_NCDESTROY RemoveWindowSubclass 合规/单测纯函数化去全局竞态）。191 测试三连绿；锁屏冒烟：启动存活+子类安装成功+WM_CLOSE 优雅退出无 panic。**已知限制**：鼠标在窗控 NC 热区时 egui hover 高亮不工作（NC 消息不进 egui，Snap 功能优先，接受）。真机交互验收（拖动/双击/拖边 resize #4186/最大化 8px/Snap 悬停/阴影圆角）在待解锁清单 | 
| F2 | **系统提示框**：应用内统一的提示/通知组件（toast 浮出、自动消失、分级 info/warn/error） | 小（0.5~1 天），egui 实现 | M3.6 已交付（`a1aa43a`）：右下角分级浮层 + 三场景打样 | ✅ 完成 |
| F3 | **热更**：应用自动更新——检查新版本 + 下载 + 自替换 | 中（方案 2026-06-13 拍板，见下「F3 热更方案」）：不依赖自建服务端，**可提前到 M5 之前独立做** | ✅ **代码完成（2026-06-13 夜间）**，待建仓+发 Release 端到端验收 | ✅ 客户端+脚本就绪 |

## 体验打磨（M3 验收反馈，2026-06-11）

| # | 问题 | 优先级 | 状态 |
|---|---|---|---|
| P1 | codex 运行时最左侧蓝色线条闪烁（光标已不闪） | 中 | ✅ 完成（`5fe783f`，真主因=蓝条下边界读 live 光标行无防抖，改防抖行 + 门控欠帧限期暂缓，详见 commit） |
| P2 | 文件树双击文件行为——海风哥确认：**保持系统默认程序打开，现状即正解** | — | 关闭 |
| P3 | 文件树增强（海风哥 2026-06-11 列举）：①拖动文件到终端区→插入路径到命令行 ②右键菜单（新建文件/文件夹、删除走回收站、打开文件管理器、复制绝对/相对路径）③树顶部搜索按钮→后台递归搜索 | 高（明确需求） | ✅ 完成（`4bde6b5`） |
| P4 | 浅色主题色板未经设计调校 | 低 | ✅ 完成（`c7671d2`，对齐 tokyonight day 官方色板，待海风哥目检） |
| P5 | 后台会话洪泛时回显延迟尖峰 10~30ms（全局通道队头阻塞） | 中 | ✅ 完成（`de5d2c2`，per-session 有界通道 + 激活会话优先 drain） |
| P6 | cargo fmt 全仓统一（单独纯格式化 commit） | 低 | ✅ 完成（`430130d`） |

### P1 蓝线闪烁诊断结论（2026-06-11）

- **根因**：命令块左缘「运行中」蓝色状态条（`lumen-renderer/src/lib.rs:425`，色值 Tokyo Night ANSI 蓝 `#7aa2f7`，绘制于 `lib.rs:408` 的 `if !term.is_alt_screen()` 分支）。codex/Claude Code CLI 跑在**主屏**（非 alt screen，用 ANSI 光标移动 + DEC 2026 同步批做行内重绘），始终是一个未闭合的「运行中」块 → 左缘常驻蓝条。
- **闪烁机理**：codex 持续输出致主屏滚动，蓝条跟随块可见行区间移动；昨夜渲染门控（`main.rs` `skip_term_render`：同步区间跳过离屏渲染、保留上一帧）令蓝条位置更新与补帧不连续 → 视觉闪烁。光标因有独立冻结防抖（`cursor_frozen_at`）跳帧时整体不画，故不闪。
- **候选 2/3 排除**：侧栏激活条（`bg_highlight #292e42`，蓝灰非纯蓝、且与终端区物理分离）、文件树交界（露底色为深灰黑非蓝）均不符。
- **修复方向（放打磨批次，碰渲染门控核心需一起回归）**：①跳帧时仍消费 `take_dirty()` 并对块矩形几何单独同步；或 ②ESU 快路直渲永不跳帧、门控只作用于静默合帧定时路径（更简单，倾向此方案）。需 LUMEN_PERF 验证修后空闲仍零渲染、无空转回退。
- **海风哥已确认（2026-06-11）**：蓝线就在 codex 文字区域最左列——候选 1 坐实。

## M3.6 验收反馈第二轮（海风哥 2026-06-11）

| # | 需求/问题 | 状态 |
|---|---|---|
| P7 | 文件夹右键菜单加「进入文件夹」（cd 过去，复用双击链路含忙闸门） | ✅ 完成（`3077f36`，树根也提供） |
| P8 | **bug**：双击文件没有调起系统程序打开（功能 M3.3 即有，实测无反应——疑 M3.6 拖拽改造吞了双击事件或打开调用本身有问题，诊断修复） | ✅ 完成（`3077f36`，根因见下） |
| F4 | 会话持久化：左侧栏会话列表关闭程序后保留（重启恢复条目与各自 cwd，重开 shell，不恢复屏幕内容）；默认标题（未重命名时）显示**当前 cwd 文件夹路径**并跟随 cd 变化——现在显示的是 shell exe 路径，不对 | ✅ 完成（sessions.json 持久化 + 标题取值统一） |

### P8 双击失效诊断结论（2026-06-11）

- **根因**：egui_ltreeview 0.7.0 上游 bug——Click 处理里单槽 output 先置
  `SetLastclicked` 随即被 `SelectOneNode` 无条件覆盖，`last_clicked_node`
  永不记录 → 双击判定 `was_clicked_last` 恒 false → 鼠标双击的
  `Action::Activate` 不可达（回车键独立路径不受影响）。自 M3.3 引入
  0.7.0 起即存在（当时验收走的回车/目录场景未暴露），**与 M3.6 拖拽
  改造无关**（egui 0.34 下 click_and_drag 的干净双击不会进入拖拽分支，
  已读源码核对）。crates.io 最新仍是 0.7.0，无升级可用。
- **排除项**：打开调用本身经函数级验证无问题——`explorer.exe <路径>`
  对裸路径与含空格/中文的引号路径均正确调起关联程序（spawn Ok +
  关联编辑器窗口标题核对）；搜索结果列表的双击走 egui 原生
  `double_clicked()`，本来就不经 ltreeview，无此 bug。
- **修复**：`merge_double_click_activation` 合成激活（整树 Response 双击
  + 本帧 SetSelected → 激活点选节点），真实 Activate 优先、上游修复后
  自动让位；附无头 egui 复现单测作上游行为哨兵（上游修复时测试失败
  提醒移除合成分支）。

## 新功能（海风哥 2026-06-11 第三轮）

| # | 需求 | 状态 |
|---|---|---|
| F7 | **分屏增强**（海风哥 2026-06-11 第五轮）：①每个窗格加**顶部标题栏**（显示该格 cwd/标题，✕ 关闭钮迁入标题栏，占一条窄栏、终端行数相应扣除）②**拖动标题栏在窗格间交换位置**（拖 A 标题栏落到 B 上 = A/B 互换，焦点与持久化同步）③**窗格自由调整大小**——布局从固定均分升级为可调比例：网格结构保持（1~6 格规则），排高比与排内列宽比可拖动（窗格间分隔条 hover 变 resize 光标、拖动调比例、双击恢复均分、每格最小尺寸钳制）；**P11 间隔线并入本项**（分隔条 = 可视分隔线 + 拖动手柄一体）；持久化升级存比例，重启还原 | ✅ 完成（M3.7c：批1 `d344654` 布局引擎可调比例+分隔条（③+P11）、`927e355` 窗格标题栏+比例持久化（①），批2 `7a9d08c` 拖标题栏换位（②）——egui 点击/拖动阈值仲裁、目标格 accent 高亮+跟手半透明标题小卡、交换内容权重不动、落盘还原） |
| F8 | **单实例限制**：正式发布版不可多开——第二个实例启动时检测已有实例（Windows 命名互斥量），把已有窗口带到前台后自身退出；测试版可多开——debug 构建默认放开，release 另留 `--multi-instance` 启动参数/环境变量逃生口便于测试 | ✅ 完成（M3.7d 批4：命名互斥量 `Local\Lumen-SingleInstance-<用户名>` + 命名事件前台化通道（监听线程置标志、借既有 PtyWake 唤醒，主循环 set_minimized(false)+focus_window+request_user_attention）；release 实测第二实例 88ms 静默退出、`LUMEN_MULTI_INSTANCE=1`/`--multi-instance`/debug 多开均放行；顺手给 settings/sessions/profile 写盘日志加 PID（B1 纵深防御，多开互踩可溯源）——肉眼前台化效果留人工验收） |
| F6 | **多语言（i18n）**：UI 文案全量抽离为语言资源，优先支持**简体中文、繁体中文、英文**；设置页加语言选择（**默认简体中文**，海风哥拍板），切换即时生效+持久化；覆盖范围=外壳全部 UI 文案（侧栏/顶栏/设置/登录/文件树菜单/toast/悬停提示/确认弹窗）；技术方案 agent 调研定夺（rust-i18n / fluent / 自制静态表，倾向轻量）；注意繁体字形（msyh 含繁体，需实测）与日后 M4 新增文案的接入纪律（新 UI 文案一律走资源表，code review 把关） | ✅ 完成（**方案调研拍板：自制静态表**——`Strings` 结构体 ~130 字段 × 三个 static 实例，漏翻任何字段编译报错（rust-i18n 4.1/fluent 均无此保证且各拖 6~10 个 crate，否决）；零新增依赖、&'static str 对 egui 零分配；AtomicU8 全局语言即时切换；settings.json 顶层 `language` 字段（BCP 47："zh-CN"/"zh-TW"/"en"，serde default 旧文件兼容）。实现 `6f51a33`（i18n 模块五文件 + 14 文件全量抽离 +1461/-192：设置页语言下拉（Appearance 顶部、选项固定原生写法）、filetree 后台线程 toast 改 OpResult 枚举 UI 侧翻译、登录校验改 LoginError 枚举、插值统一 fmt1/fmt2 模板工具（format! 不接受运行时模板））+ 评审修复 `1c1a561`（三路评审 18 条验真 12 条实修 6：启动路径 font_hint/写盘失败 toast 两处硬编码中文漏网 major、zh_tw「回退」大陆用语→「還原」、en Sign in/Log in 不一致、en 删除确认句法缺冠词、LoginUiState 错误缓存 String→枚举防语言切换后过期）。新增文案纪律已写入 i18n/mod.rs rustdoc：加字段→三语必填→编译器把关。**验证**：179 测试三连绿；真机三场景（默认 zh-CN / 预写 zh-TW / 预写 en）启动加载日志验证 + en 重启持久化 Before/After PASS + 4 份日志零 panic/ERROR；msyh.ttc 字形覆盖检查 24 个关键繁体字全有（豆腐块风险排除）。**遗留待人工/解锁补验**：三语 UI 视觉目检与语言下拉真实点选交互——验证时段屏幕已锁定（LogonUI），截图/注入不可用（workflow agent 误归因 Hyper-V，实为锁屏），见「待解锁验证清单」） |
| F5 | **终端分屏**：终端区右侧 + 按钮新增窗格，每个侧栏 tab 内最多 6 格；布局规则固定均分——1=满屏 2=左右 3=左中右 4=上2下2 5=上3下2 6=上3下3；点击窗格聚焦（accent 边框指示），键盘/IME/文件树跟随焦点窗格；窗格数量与各自 cwd 持久化、重启还原（sessions.json 结构升级+旧格式迁移）；shell exit 关窗格，快捷键 Ctrl+Shift+D 增 / Ctrl+Shift+W 关 | ✅ 完成（M3.7：批1 `1e54d35` 数据模型/多窗格渲染/布局/焦点路由，批2 `82f1b3d` 顶栏＋按钮/悬停✕/拖放落点窗格 + 持久化 v2 与文档收尾） |

## 样式反馈（海风哥 2026-06-11 第四轮）

| # | 需求 | 状态 |
|---|---|---|
| P9 | **外壳 UI 纯黑白化**：去掉蓝色 accent，按钮等控件改黑白灰高对比（对标 Warp 本尊观感：主按钮白底黑字、激活/焦点用白与亮灰、hover 走亮度梯度）；深浅双主题同步；终端 ANSI 16 色与块状态条（蓝运行/绿成功/红失败语义色）保留不动 | ✅ 完成（M3.7b：shell/theme.rs 双色板黑白重做+控件三档亮度梯度+CTA 白底黑字，egui 蓝默认值（链接/选区/光标）全去；附带终端选区改中性灰——不喜欢可单独回退，见 lumen-renderer/src/theme.rs selection 字段文档） |
| P10 | **侧栏宽度可拖动**：左侧 tab 栏、中间文件树栏支持左右拖动调宽（egui SidePanel resizable + 合理 min/max），宽度持久化到 settings.json 重启保留 | ✅ 完成（M3.7c 批2②，即本条目所在 commit：egui Panel resizable，tab 栏 140~320 / 文件树 160~480 逻辑 px；宽度松手落盘 settings.json `layout` 节（serde default 兼容旧文件）、重启还原；拖宽手柄 raw 鼠标让位不抢终端焦点） |
| P11 | **分屏间隔线**：窗格之间画好看的分隔线（现为 2px 空隙露底色）——1px 灰阶细线居中、像素对齐，色值随 P9 黑白化色板（深色主题亮灰/浅色深灰），hover 不需要交互（布局固定均分） | ✅ 完成（M3.7c 批1 `d344654`，并入 F7③：1 物理像素 bg_highlight 细线居中像素对齐，随 F7 升级带拖动手柄（hover/拖动 resize 光标、双击均分）——「不需要交互」被 F7③ 覆盖升级） |
| P12 | **主题皮肤库**（对标 docs/截图/设置-外观.png）：①内置主题集 10 个左右（黑白默认/Tokyo Night 深浅/Dracula/Nord/Gruvbox/Solarized 深浅/Catppuccin/One Dark…），每个=终端 16 色+bg/fg/selection + 外壳色板联动 ②设置-外观页改 Warp 版式：主题预览卡片画廊（迷你终端配色缩略图）+ 当前主题展示 + Sync with OS（跟随系统深浅自动切换）③选择即时生效+持久化。**后置不做**（等点名）：窗口透明度/毛玻璃、Input type、Dim inactive panes、Blocks 开关组、ligatures/行高 | ✅ 完成（M3.7e：批1 `6dbf776` 主题注册表 11 个——Lumen Dark/Light=P9 现状配色提炼命名（保真不走派生）+ Tokyo Night 深浅/Dracula/Nord/Gruvbox Dark/Solarized 深浅/Catppuccin Mocha/One Dark 官方色板（联网抓官方 repo 校对，来源 URL 注释在 lumen-renderer/src/themes.rs）；外壳色板自动派生（终端 bg/fg/ANSI→灰阶阶梯保主题色相，accent 守黑白化白/近黑，亮度单调性+对比度下限单测）；settings.json v2：theme 改 id 字符串+version 版本号，旧值 tokyo-night(-light) 迁移 lumen-*（官方版为新条目）+未注册 id 字段级降级。批2 外观页 Warp 版式：Themes 组（Sync with OS 开关、当前主题展示卡、画廊网格卡片=painter 画迷你终端色条缩略图+名字，当前主题 accent 描边，点卡=终端+外壳整套即时生效+落盘）+ Text 组保留；Sync with OS 用 winit window.theme()/WindowEvent::ThemeChanged 零新增依赖，深/浅槽位各指定（sync 开启时点深/浅色卡片分别写对应槽，卡片带「深色/浅色」徽标）。E2E：11 个 id 预写 settings 启动逐一验证「设置加载：主题 X（id y）」日志、真实旧格式文件迁移日志验证、8 秒无 panic；OS 深浅切换的运行时即时响应留人工验收） |

## 外观增强（海风哥 2026-06-11 第六轮）

| # | 需求 | 状态 |
|---|---|---|
| P13 | **终端背景图片**：外观设置加背景图替换功能——选本地图片（rfd 文件对话框）+ 暗化/不透明度滑块（保文字可读）+ 开关，settings.json 持久化；实现方案 agent 评估二选一：①终端离屏纹理默认背景写透明 alpha，背景图在 egui 层画在终端纹理底下（蒙层同层，渲染管线近零改动，倾向此方案）②渲染管线内加背景图 pass；作用范围=终端工作区整体（跨窗格一张图），外壳侧栏/顶栏不透 | ✅ 完成（M3.7f：方案①拍板——窗格离屏纹理通路现成、默认底色 cell 本就跳过绘制，管线近零改动；方案②需新增 pass + 跨窗格 uv 对齐，否决。批1 `4f27de9` BackgroundSettings + 渲染透明通路（离屏 Clear 按 `transparent_background` 切 `wgpu::Color::TRANSPARENT`，RGB=0 防 premultiplied 加色泄漏）+ 图片加载模块（image crate 解码 png/jpeg/webp/bmp、premultiply、cover 居中裁剪 uv、>8192 边长拒绝）；批2 `bbd7c58` 设置页「背景图片」组（开关 + rfd 选图 + 不透明度 5%~100% / 暗化 0~90% 双滑块松手落盘 + 清除）+ 信号联动；评审修复 `0072fb8` 三路评审 12 条 finding 验真确认 7 条全修——blocker：背景图纹理 `Rgba8UnormSrgb`→`Rgba8Unorm`（egui 着色器期望非 sRGB-aware 纹理，原格式 GPU 反伽马+egui 二次转换画面偏暗）；major：设置页 open_with 未清零滑块拖动残留；minor×5。验证：三连绿 162 测试；真机 E2E jpeg/png/负路径（不存在文件降级 ERROR 不 panic）三场景日志全过、用户配置备份还原；**初轮视觉核验误判记录**：用深色 Warp 截图 + opacity0.5 + dim0.3 在近黑底上不可辨被误判「未生效」，换鲜艳四色块测试图 opacity=1 裁决——背景透出/外壳不透/文字叠加/标题栏分隔条盖图全部正确（取证截图 target/p13-verify/p13-vivid.png）。已知接受项：rfd 同步对话框阻塞事件循环（关闭后 PTY 恢复消化，代码注释在案）） |

## 分屏反馈第二轮（海风哥 2026-06-11 第七轮）

| # | 需求/问题 | 状态 |
|---|---|---|
| B1 | **bug**：窗格比例与侧栏宽度**重启不还原**（M3.7c 自验只看了加载日志，未验真实交互写盘路径与 UI 应用路径——两面都要诊断：拖动后文件是否真变 / 启动后 UI 是否真应用） | ✅ 完成（M3.7d 批1：**单实例下两面均验证通过**——窗口消息级真实拖动 E2E：拖分隔条→drag_stopped 落盘 0.634/0.366、拖侧栏→松手落盘 230px→WM_CLOSE→重启，「外壳布局应用」日志证明 UI 真用上持久化值。现场复盘最可疑根因=**多实例互踩**：后台旧实例的 cwd 上报/退出落盘会用自己内存里的旧布局覆盖 sessions.json（F8 单实例已立项，正解在彼）。本批加固：①比例写盘加 layout_dirty「指针松开兜底」——drag_stopped 边角错失（拖动中失焦等）也必落盘 ②启动首帧 INFO 输出实际应用的侧栏/文件树宽与窗格权重（恢复面验收闭环，重启肉眼可查）③宽度写盘判定抽纯函数 width_worth_persisting + 单测（亚像素/瞬态压缩/NaN 不写）④debug 级拖动/落盘日志 + LUMEN_DIAG_IGNORE_CURSOR_LEFT 诊断开关（无交互桌面自动化拖动测试用）） |
| P14 | **窗格最大化/还原**：多窗格时每格标题栏加最大化按钮——点击占满整个命令行区、其它窗格隐藏（仍后台消化输出）；变还原按钮，点击回原布局其它窗格还原；最大化态新增/关闭窗格自动退出；状态持久化重启保持；顺手加 toggle 快捷键 | ✅ 完成（M3.7d 批2：标题栏 ✕ 左侧 ▢/⧉ painter 画线按钮（多窗格时显示，tooltip 标注快捷键）+ Ctrl+Shift+Enter toggle（守卫同 Ctrl+Shift+D/W）；最大化=该格占满终端工作区、焦点强制为它，其余格矩形 NOTHING 占位——不渲染不 resize 不进鼠标/IME 路由，后台照常消化与回写（同「非激活 tab」闸门），还原时强制补帧；分隔条隐藏、拖动换位禁用；＋/Ctrl+Shift+D 增格自动退出、关格自动退出（关隐藏格下标左移）；Tab.maximized 入 sessions.json（serde default 兼容、读侧越界/单格夹紧 None），重启保持且布局权重原样还原。E2E：预写 maximized=1 启动→「外壳布局应用…最大化=Some(1)」→点 ⧉ 还原（权重 0.3/0.7 不变）→点 ▢ 再最大化（focused/maximized 同步落盘）） |
| P15 | **一键恢复默认布局按钮**：顶栏加复位按钮（＋旁），点击=当前 tab 全部窗格比例恢复均分；单窗格时禁用 | ✅ 完成（M3.7d 批3：顶栏「＋」左侧「▦」按钮（tooltip「恢复窗格默认大小」，单窗格禁用态 + 禁用悬停提示）；点击=激活 tab 行/列权重全部均分，最大化态先退出（其余格还原可见并强制补帧），复位后落盘；已均分且非最大化时无变化不写盘。E2E：预写 0.3/0.7+maximized=1 → 点 ▦ → 落盘 [0.5,0.5]+maximized=null、窗格数不变） |

## 验收回归 bug（海风哥 2026-06-11 第八轮，验收不通过）

| # | 问题 | 状态 |
|---|---|---|
| B2 | **多窗格渲染回归**（截图证据）：①多数窗格**第一行不显示文字**（块条/光标竖条可见、提示符文字缺失）②焦点窗格有时显示但**内容错乱**（提示符开头丢字符、输入回显与路径文本错位叠加、光标位置不对）。时间锚点：M3.7（F5 分屏）验收时多窗格正常 → 回归引入于 d344654..e71a158 之间（标题栏占高/B1 门控/P14 隐藏格/P12 主题之一） | ✅ 修复（三路诊断交叉裁决：**症状①根因=Grid::resize 缩行无条件把顶行搬进 scrollback**——新鲜提示符窗格缩行后提示符进历史、可视区全空，而块条/光标按元数据照画=「两根竖条」；M1 潜伏缺陷，M3.7c/d 的标题栏占高/整区估算 spawn/分隔条逐帧 resize 把触发面扩成常态，区间内 lumen-term/renderer 零改动、渲染器无辜。**症状②根因=resize 风暴×ConPTY/PSReadLine 差量渲染坐标失步**，错乱字节在 grid 里、渲染器忠实传话。P14 最大化越界嫌疑核实不可达（close_pane 正确调整下标+截图焦点格有上屏）排除。修复 commit：**dcde941**（lumen-term：缩行优先收割光标下方空行+超限丢行 dropped_lines 漏计修正+5 单测）、**b6abd52**（lumen-app：恢复路径按布局权重逐窗格估算 spawn 尺寸——实测 5 格恢复首帧 resize 为零；分隔条拖动暂缓 resize 松手一次提交——实测注入 12 帧拖动中零 resize；pty.resize 失败 warn 可观测+3 单测）。验证：三连绿 141 测试、release 5 窗格上3下2 恢复 12s 无 panic、空闲零渲染保持） |

## 样式微调（海风哥 2026-06-11 第九轮，参考截图已存 docs/截图/）

| # | 需求 | 状态 |
|---|---|---|
| P16 | ①左侧会话栏、中间文件树栏、右侧命令行区**各加 1px 灰白色描边**（面板轮廓细线，参考截图观感，像素对齐，色值入黑白色板做主题联动）②**命令行区背景改黑色**——当前默认主题 Lumen Dark 终端 bg 是 Tokyo Night 的 #1a1b26（蓝紫调），改近黑（外壳底色已近黑，终端区跟齐；ANSI 彩色在黑底对比无碍；仅 Lumen Dark/Light 默认主题调整，其它主题保官方原色） | ✅ 完成（描边：`panel_outline` 色板新字段（深色 #626262 / 浅色 #888888）+ 派生联动；三栏各自 `1/ppp` 物理像素 `StrokeKind::Inside` 内描边像素对齐；黑底：Lumen Dark `bg` 改 `#0d0d0d`（选值：接近纯黑保留质感、与外壳 #161616 形成内凹层次），`Theme::default()` 单一改点联动注册表、主题预览卡片、renderer Clear 色。三连绿 141 测试通过） |

## 复验反馈（海风哥 2026-06-11 第十轮）

| # | 问题 | 状态 |
|---|---|---|
| B3 | **打字仍错乱**（B2 修复后复验截图：`PS F:\Clls▌-l orkspaces\engram>`——提示符丢字+输入混叠同款症状）。B2 仅堵了拖动 resize 风暴；本轮必须**字节流取证**定根因：①错乱自延续假设（PSReadLine 屏幕假设失步后持续，cls 才恢复？）②残余 resize 源（文件树开合/窗口/最大化切换）③ConPTY resize 重发整屏与本地 Grid reflow（含 B2 修① 新缩行语义）叠加错位 ④消息注入打字复现 + PTY 原始字节流落盘对照 grid 终态，判定「shell 发来即错」vs「本地应用错」 | ✅ 修复（**五次根因定位，B3-5 终结「旧窄格折行锚点留存」混叠**）<br><br>**B3-1（`e8d8a65`）**：new_pane() 预计算首帧尺寸，消除「新建窗格第一提示符行」错乱（首帧零 resize）。<br><br>**B3-3（`14d7aab`）**：Grid::resize 扩行优先从 scrollback 拉回历史行（ConPTY 视口语义对齐），消除「最大化后打字错乱、回车恢复」的坐标失步主因。<br><br>**B3-4（`22f2d53`）**：Grid::resize 扩行分支先 resize cols 再 push_front 历史行，消灭 OOB panic。<br><br>**B3-5（本轮，现场字节流钉死）**：字节流取证（`target/b3dump-user/pane-1.txt`）证实 ConPTY 重发流完全正确（`ESC[8;18;36t`→`ESC[H`→清空→`ESC[2;32H` 光标归位正确），错误由 PSReadLine 自身产生：`ESC[2;4H` 把输入写到第 2 行**第 4 列**（旧 14 列宽折行后 31 字符提示符停在第 4 列——PSReadLine `_initialX/_initialY` 锚点在窄格折行时记录，最大化解折行后 ConPTY 重画了正确画面但 PSReadLine 内部锚点未更新）。**缓解方案**：resize 成功且窗格 `shell_waiting_input()==true` 时，300ms 后向该窗格 PTY 注入 F13 VT 序列（`ESC[25~`，即 `F13_VT_SEQ`）；PSReadLine 处理此键时设 `_handlePotentialResizing=true` → `ProcessOneKey` → `ForceRender` → `RecomputeInitialCoords`，以新 `BufferWidth` 重算锚点。F13 无默认绑定，`SelfInsert` 对 `KeyChar='\0'` 直接返回，对命令行内容零副作用。**调研结论**：WT issue #2432/#15042 / PSReadLine #1456/#3074 证实此为跨终端已知问题（WT、VSCode 等均有报告），PSReadLine ≥2.2 的 `RecomputeInitialCoords` 能正确处理宽度变化但必须经 `ProcessOneKey` 触发；注入 F13 即为此触发机制的终端侧缓解。<br><br>**残余限制**：注入依赖 `shell_waiting_input` 的 OSC133;B 标记，未注入 shell integration 的会话无此标记——此路径注入跳过，那些会话维持旧行为（非常规使用）。分隔条拖动期间已由 `divider_resize_held` 暂缓 resize，注入不会在拖动中触发，松手后第一帧 resize 才计划注入。<br><br>**B3-5b（`ebdffdc`，最终方案——F13 方案经主会话上游源码复核否决后改造）**：F13 无效的三条实据：①F13 无绑定 → SelfInsert 收 `\0` 直接 return，根本不调 Render/MoveCursor，`RecomputeInitialCoords` 不可达；②即使触发渲染，`RecomputeInitialCoords` 的重算只是 `_initialX %= BufferWidth` 取模（3%36 仍是 3，正确值 31）——**解折行场景它不重新测量提示符，算不对**；③这也解释了用户打字第一键（真实 Render）也没修复锚点的现场观察。**唯一真正重建锚点的时机 = shell 重发全新提示符**（用户实测「空行回车恢复」背书）。改造为**空行回车注入**：resize 稳定 300ms 后，对「`shell_waiting_input()==true` **且** 自本提示符起用户零输入（`user_input_since_prompt==false`，全部 5 条用户写 PTY 路径经 `write_user_input()` 收口跟踪，提示符出现沿清零）」的窗格注入 `\r` → shell 在新宽度下重发全新提示符 → PSReadLine 锚点彻底重建。代价=resize 后多一行提示符（诚实代价）。**真机回归通过（2026-06-11 21:09 build）**：恢复 7 窗格窄格布局→最大化→6 个等待输入窗格全部收到注入（info 日志）→ dump 实证注入回车回显后跟随全新 OSC133 提示符周期（36 列新宽度下生成）。**最终残余**：用户敲了半行未回车时 resize，该行仍错乱至他自己回车（守卫不清用户输入，自愈）；无 shell integration 的会话不注入。<br><br>**B3-6（B3-5b 实测缺陷修复，本提交）**：海风哥截图实证两个缺陷：①反复放大缩小提示符行堆积（每轮注入一次，堆出 4+ 行 `PS F:\...>`）②缩放往返滚动历史丢失（scrollback 逐步蒸发）。**修复三件套**：(1) **撤销 B3-3 扩行拉回逻辑**（`grid.rs` resize 扩行分支改回「底部 push_back 空行、scrollback 只进不出」）——ConPTY 重发流负责可视区对齐，历史从此守恒；4 个 B3-3/B3-4 单测按新语义改写；(2) **注入加精准条件**（`main.rs`）：`prompt_is_wrapped()` 折行判定（OSC 133 B 行 > A 行才需注入，不折行的宽格零注入） + `RESIZE_INJECT_THROTTLE=2s` 节流（同一窗格 2s 内不重复注入）；(3) **`prompt_is_wrapped()` 新 API**（`term.rs`）：4 个单测，在 lumen-term 暴露折行查询。**真机回归**（2026-06-11 21:26 build）：场景 B（7 格窄格最大化）注入 6 次（1 轮）、立即二次 maximize 零额外注入（节流/折行判定双重生效）；场景 A（宽格单次 resize）debug 日志确认「提示符不折行，跳过注入」。三连绿 155 测试全通过。<br><br>**B3-7（海风哥拍板，彻底拆除注入机制）**：B3-5/B3-5b/B3-6 引入的 resize 回车注入方案经海风哥实测最终否决——即使加了折行判定 + 节流，实测仍产生多余提示符行，体验不可接受。海风哥明确拍板：不要注入回车。**拆除清单**：`RESIZE_REDRAW_SEQ` / `RESIZE_INJECT_DELAY` / `RESIZE_INJECT_THROTTLE` 三个常量；`AppState.resize_inject_pending` 字段；`about_to_wait` 注入执行块；`RedrawRequested` 注入计划块；`Session` 的 `shell_waiting_input_last` / `last_inject_at` / `user_input_since_prompt` 三个字段及其维护逻辑；`Terminal::prompt_is_wrapped()` API；`b3_5b_*` 三个单测（`term.rs` 中 `b36_提示符折行/不折行/执行中/备用屏幕` 四个注入专属单测）。**保留**：B3-6 的历史守恒修复（`grid.rs` scrollback 只进不出 + 4 个 `b36_*` scrollback 守恒单测）；`write_user_input()` 包装方法（保留统一收口价值，去掉守卫字段）；`LUMEN_DUMP_PTY` 取证设施（env 门控，长期排障）。**已知限制（如实记录）**：窄窗格提示符折行后经历宽度变化，当前提示符行打字会错位至用户回车自愈。根因为 PSReadLine 上游缺陷（锚点不随解折行重测），WT #2432/#15042 同款，终端侧无非侵入手段，接受现状。如未来要治：设置页加可选开关（默认关），用户手动启用注入。三连绿 148 测试全通过。<br><br>**B3-8（本轮，海风哥新观察推翻 B3-5 单因论）**：**B3-5 PSReadLine 定性修正——上游缺陷场景真实存在但非主案**；海风哥新观察确认主案为：**整窗 resize 路径下窗格 term/PTY resize 未执行**（文字仍按旧窄宽折行，新宽度根本没送达 shell）。根因定位：`divider_resize_held` 门控（`main.rs:2738`）在 `WindowEvent::Resized` 触发的帧中可能为 `true`——当 egui 拖动状态因 `LUMEN_DIAG_IGNORE_CURSOR_LEFT=1`（自动化测试）或指针事件被系统接管（Windows 模态 resize 循环、焦点切换）导致 `PointerGone` 未送达时，`divider_drag.is_some()` 持续为 `true`，整窗 resize 产生的每一帧都被挡在 `!divider_resize_held` 判定外，term/PTY resize 永久不执行。**修复**（`main.rs`）：新增 `window_just_resized: bool` 标志，`WindowEvent::Resized` 与 `WindowEvent::ScaleFactorChanged` 处理时置位；`RedrawRequested` 的窗格 resize 路径消费并清零此标志，凡携带 `window_just_resized=true` 的帧（OS 级整窗 resize）强制绕过 `divider_resize_held` 门控——整窗 resize 与分隔条拖动完全独立，无需暂缓 ConPTY resize。新增"暂缓原因"诊断日志（`divider_resize_held=true 暂缓`）供后续取证。**验证**：`b3_8_run.ps1` 场景 A（`LUMEN_DIAG_IGNORE_CURSOR_LEFT=1` + 注入拖动 + SetWindowPos 连续 resize）和场景 B（干净启动 + SetWindowPos resize）均在 debug 日志中产生「窗格 id=N 网格 MxC → RxC」多行（如 `16x27→17x29`、`17x29→18x32`），dump 文件含 `ESC[8;...t` resize 序列；fix 前场景 A 该日志缺失（复现成功）。三连绿 47 测试全通过，build 时间戳 2026-06-11 22:28。 |
| P16b | 描边三连修：①会话栏与文件树栏之间**双边 2px**（相邻面板各画内边视觉叠加）→ 共享边只画一条 ②描边太白 → 调暗成灰（#4a4a4a 量级目检定）③**命令行区边框没显示**（层级/时机被内容盖住）→ 画到内容之后或 Foreground 层 | ✅ 完成（`62e6baf` 三连修 + `fbf7064` 复验修正：共享边去重规则改「左侧面板画右边线」，海风哥确认边框回来了） |

## 窗口行为（海风哥 2026-06-11 第十一轮）

| # | 需求 | 状态 |
|---|---|---|
| P17 | **启动默认全屏**：app 窗口启动时默认最大化（Windows 语义 maximized，带任务栏，非无边框独占全屏）；取消最大化后还原为 1000×640 默认尺寸 | ✅ 完成（窗口创建 attrs 加 `with_maximized(true)`，与 P13 同批 commit；真机验证启动即最大化） |

## P13 验收反馈（海风哥 2026-06-11 第十二轮）

| # | 问题 | 状态 |
|---|---|---|
| B4 | **bug**：背景图不透明度/暗化滑块没有持久化，重启即重置 | ✅ 修复（根因：滑块为即时预览在**拖动中实时写 settings 字段**（与字号滑块「只存暂存值」模式不同），导致松手帧的变更对比基准已被上一帧预览值污染——`(before - preview) > EPSILON` 恒 false，`background_params_changed` 永不置位、从不写盘；本次运行生效（内存已改）但重启读盘上旧值。修复：松手即无条件置落盘信号（点击未改值多写一次原子盘，无害换确定性），settings_ui.rs 注释记录陷阱。**真机交互验证闭环**（B1 纪律：两面都验）：注入真实鼠标拖动不透明度 0.40→0.85、暗化 0→0.30 → settings.json 实时落盘新值 → 优雅退出盘值不被覆盖 → 重启背景图按新值渲染（截图对比明显）。教训：评审 12 条+E2E 三场景都没抓住它——E2E 只验了预写加载面，滑块真实交互写盘面缺位，与 B1 同型） |

## 排期决策（2026-06-11 更新）

1. ~~M3.6 打磨批次~~ ✅ / ~~M3.6b~~ ✅（均验收通过）
2. ~~M3.7：F5 终端分屏~~ ✅（必须在 M4 前做的前置已就位：M4 输入编辑器是 per-窗格设计）
3. **M3.8**：F1 自绘标题栏（与顶栏融合）
4. **M4**：输入编辑器（设计已定稿 `docs/输入编辑器设计.md`）
5. F3 热更挂 M5 前后另行拍板

## B3 最终结案（2026-06-11 海风哥拍板）

- resize 链路侧 4 个真 bug 已全部修复（B2 风暴/B3-1 新建窗格尺寸/B3-4 扩列越界/B3-8 整窗 resize 被分隔条暂缓状态滞留挡住——最后一个是海风哥主案），ConPTY 字节流确认新尺寸送达。
- **残余接受现状**：resize 时提示符行恰处折行状态 → 该行打字错位 → 回车自愈。根因 PSReadLine 上游缺陷（2.4.5 最新版未修：RecomputeInitialCoords 仅对 X 取模、不重测提示符；WT #2432/#15042 同款）。回车注入方案经实测否决并已全部拆除。
- **备选方案待拍板（新会话可接力）**：提示符短化（integration.ps1 改 `PS> ` 短提示符 + 设置项开关，cwd 已在窗格标题栏显示）——大幅缩小触发面，海风哥暂未拍板，列停车场。
- 沉淀设施：LUMEN_DUMP_PTY 字节流取证（env 门控）、target/b3_8_repro.ps1 注入复现框架。

## M4 输入编辑器进度（2026-06-12 凌晨，海风哥授权夜间自主推进）

| 分期 | 状态 | 内容与证据 |
|---|---|---|
| P0 地基 | ✅ 完成 | `fa8ef17`+`92b25e6`：PSReadLine 驯化（逐条 try/catch）入 integration.ps1；shell_waiting_input 补 5 单测（B3 期已有实现）；**OSC 633 透传实测=被系统 ConPTY 吞**（取证测试入库 #[ignore]，M4.2 权威命令文本定案 133 私参 base64 降级，结论已记设计文档 §3.3）。cwd 上报链路 P0 前已就位（侦察核实零工作）。 |
| M4.1 底座 | ✅ 代码层完成 | 批A `d83b39d` **lumen-editor crate**（纯状态机：行存储/grapheme 光标（unicode-segmentation 选型）/undo 合并/Action serde/replay 确定性测试，67 测试）；批B `b0d29da` **四态模式机纯函数 + Action 总线 + keymap 静态表**（main.rs 八层 if-else 1:1 平移，Ctrl+C 安全规则表头+专项测试，Ctrl+Shift+E 经典直通开关，零行为变化重构）；批C `91283e4` **footer 渲染基建 + grid 扣高 + `input-editor` feature flag**（ComposerView 纯数据/RectInstance+独立 glyphon/常驻等高铁律/flag 剔除构建双验证）；批D1 `788726a` **编辑器开闸**（Compose 态键位全接管、提交六步链路（多行 bracketed paste）、IME Commit 分流、Ctrl+C 三级+空缓冲下穿 ETX 逃生舱）；专项 `6e9635b` **bracketed paste 多行 ConPTY 实测通过**（整体一次执行非逐行，E3 语义满足；坑位：PSReadLine 初始化 DSR 阻塞等应答）；批D2 `5d7eae8`+`2679ea1` **历史库**（jsonl 追加写+块闭合回填+退出原子重写+PSReadLine 种子导入（真机实证 5000→去重 2251 条）+↑/↓ 导航+放弃稿找回）+ **退出码角标** + **IME preedit 内嵌**+set_ime_cursor_area 跟随 + **100ms 增高防抖**；清理 `9b1a5cf`。测试 196→**365 全绿**，全程 clippy 0 warning。锁屏冒烟 5 轮全过（footer 占高 30px 生效/无 resize 风暴/种子导入/优雅退出）。 |
| M4.2 编辑体验 | ✅ 主体完成（2026-06-12） | **IME preedit 内嵌 / footer 鼠标定位·拖选·双击选词 / 剪贴板键位**已在 M4.1 批D2 与第十一~十三轮打磨落地；本轮补齐两大核心——批1 `849a291` **lumen-editor PowerShell tokenizer**（命令/关键字/参数/变量/数字/字符串/操作符/注释/文本 9 类语义）+ 跨行状态机（块注释 `<##>`·here-string `@""@`·跨行引号）+ `needs_continuation()` 续行判定，一套 lexer 同服高亮与续行（19 单测）；批2 `a765ddb` **footer `set_rich_text` 上色**（颜色从当前主题 ANSI 16 色派生：命令蓝[4]/关键字品红[5]/参数青[6]/变量红[1]/数字黄[3]/字符串绿[2]/注释灰[8]，11 主题自动协调）+ **Enter 续行接线**（未闭合自动换行而非提交）；验收反馈 `8038229` 参数色 `Rgb::lighten(0.35)` 提亮（青在近黑底偏沉）。顺带 `5b29979` 修复第十七轮 ghost 遗留的 `--no-default-features` 编译回归（`LookupResult::AcceptGhost` 变体漏加 cfg 致 match 非穷尽）。**剩余低优先 → ✅ 收尾完成（2026-06-13 夜间）**：OSC 633;E 权威命令文本对账（设计 §3.3，此机 ConPTY 吞 633→走 133 私参 base64）已实现——`Block` 加 `cmd_text` 字段；`integration.ps1` ReadLine 包装在 `OSC 133;C` 私参位挂 `base64(UTF-8(命令行))`（base64 纯 ASCII + 经 `-EncodedCommand` 注入，无 D1 编码坑，try/catch 兜底不吞命令执行）；`term.rs` C 标记解析 base64→`cmd_text`（lumen-term 自实现 base64 decode + 单测，无此参数/非 UTF-8 优雅降级）；`history.rs::reconcile_text` 块闭合时与编辑器记录对账，不一致（历史展开 `!!` 等）以 shell 为准校正内存条目（退出原子重写落盘）。lumen-term 60 测试 + history reconcile 3 测试 + base64 decode 测试全绿。**取舍**：仅做 value#1（对账校正既有历史条目，纯增量不改行为）；value#2（Fallback/直通态从 cmd_text 补记历史）基础设施已就位但暂未接线（属行为改变，需真机验证，留后续按需开）。三连绿全过；真机验收高亮观感 + 续行体感 + 参数色提亮均通过（海风哥 2026-06-12）。**注：参数色那批曾踩 cargo 指纹陷阱——普通 `cargo build` 重链旧 renderer 产物、改动未生效，`cargo clean -p` 强制重编才真上屏**。 |
| M4.3 历史搜索 | ✅ 完成（2026-06-13 夜间，海风哥授权拣回做） | **架构判断：M3 外壳后浮层都走 egui，不需设计稿 §7.2 的 wgpu overlay**。commit `298bc32`：`history.rs` fuzzy_search 引擎（子序列匹配 + 连续/词首/短文本/近因评分 + 高亮区间，4 单测）+ `shell/history_search_ui.rs` egui Modal 面板（搜索框 / 退出码 ✓✗ 徽标 / match_spans 命中高亮 / cwd 尾目录 / ↑↓ 导航 / Enter 填入编辑器 / Esc 关闭）+ 三语 i18n + 全套集成（Ctrl+R 打开、键盘归 egui）。三连绿；**真机验收通过（海风哥 2026-06-13）**。验收反馈 6 项已修 `d441402`（经典模式 Ctrl+R 也开 / 去重 / 频率排序限 20 / 去 cwd 列 / 键盘滚动跟随 / 文字居中；附 Ctrl+R 模式门控收窄=Compose+Fallback 开、Running/AltScreen 直通不抢 vim·REPL）+ hover 门控 `997c052`（鼠标静止不抢键盘选择）。 |
| M4.4 补全 | ✅ 完成（批1+批2，2026-06-13 验收通过） | **批1 `b99c877`（文件路径补全）✅**：`completion.rs`（current_token 提取 + complete_path 路径补全：拆目录/前缀·读目录·大小写不敏感·目录在前排序·目录加 /·隐藏文件点前缀才列，10 单测）+ `shell/completion_ui.rs` egui Area 锚定弹窗（候选列表 / 目录 accent 区分 / ↑↓·Tab 选择 / Enter·Tab 接受 / Esc）+ Tab 触发集成（接受 = SetSelection 选中 token + InsertText 替换）。anchor 首版锚 footer 上方（精确跟光标列后续）。三连绿；视觉待验收。<br>**批2 `cb0bd5a`（sidecar 命令补全）✅**：`completion_server.ps1` 持久 pwsh 进程（JSON 行协议 + `CompleteInput`，协议真机验证 Get-Ch/ls-/Remove-Ite）+ `completion_sidecar.rs`（`-EncodedCommand` 内联分发不落地 + Windows `CREATE_NO_WINDOW` 隐藏窗口 + 独立 stdout 读线程 `proxy.send_event(PtyWake)` 唤醒 + `reader_dead` 崩溃自愈 + char→byte 换算）+ `Completion.replace_range`（命令补全按 sidecar ri/rl 字节区间替换，文件路径走 current_token）+ ComposeTab 发请求·user_event 合并去重·accept 分流。三连绿（test 347）；**sidecar 真机验收通过（海风哥 2026-06-13）**。已知降级：pwsh 不可用 / 无候选时 Tab 静默无反馈（可接受）。 |

**重要**：M4.1 默认开启（`input-editor` 编译进 default features）——**Compose 态输入已被编辑器接管**，体验与之前不同属预期；不适应可 **Ctrl+Shift+E** 一键切回经典直通模式（toast 提示），或构建时 `--no-default-features` 整体剔除。

## 分发修复与体验打磨（2026-06-13，海风哥拷贝到他机分发测试触发）

| # | 项 | commit | 说明 |
|---|---|---|---|
| D1 | **shell 集成改 `-EncodedCommand` 内联注入（分发关键修复）** | `c52a8f6` | 拷 exe 到他机（用户 ivan）报 PowerShell `ParserError`（`UnexpectedToken }`）。真因=`integration.ps1` 含中文注释 + UTF-8 无 BOM 落地 Temp，被对方机（西文系统 / Windows PowerShell 5.1）按系统 ANSI 代码页解码成乱码、撑乱脚本。改为 base64(UTF-16LE) 的 `-EncodedCommand` 内联注入：不落地文件（免「找不到」）+ 不读 .ps1（ExecutionPolicy/组策略管不到）+ 精确传字节（免编码乱码）。base64 自实现零依赖；按平台分派（非 Windows 返回空参数，修原先把 pwsh 参数喂 bash 的 bug）。**分发真机验证通过**。 |
| D2 | **状态栏经典模式按钮压线** | `11a376f` | 按钮始终贴栏顶压描边线。真因=egui horizontal 内 `Align::Center`/`max_rect`/`available_height` 都拿不到真·栏高（四轮才定位）。修=进 horizontal 前捕获 `available_rect_before_wrap()` 全栏矩形、用其中心绝对定位 + `ui.interact`；栏高 24→28px（上下各 5px）。 |
| D3 | **单窗格不显示关闭/最大化按钮** | `34db511` | 推翻 F7①「单窗格也显示 ✕」旧裁决——✕ 与最大化合并进 `panes.len()>1`，单窗格标题占满、无按钮（关单窗格仍走 Ctrl+Shift+W）。 |
| D4 | **shell `exit`：单窗格原位重启、多窗格关该格** | `5185ae7` | 真因=`PtyEvent::Exited` 漏报——读线程只在 read EOF 发退出，但 Windows ConPTY 下 shell 退出后 master read 常不返回 EOF（conhost 保持 pipe），退出事件丢失、窗格卡死无响应。修=lumen-pty 新增 wait 线程直接 `child.wait()` 等退出再发 Exited（读线程 EOF 兜底、按 id 去重）、`PtySession` 改持 ChildKiller + AtomicBool。app 侧分流：多窗格关该格、单窗格 `respawn_pane` 原位重启新 shell（沿用行列/cwd、换 id、释放旧渲染资源）。 |
| — | ivan 机命令行无 PSReadLine 联想 | — | 确认是 PowerShell 5.1 自带 PSReadLine 2.0 无 prediction 功能（环境局限），Lumen 侧无解，不管它。footer 编辑器 ghost 是 Lumen 自实现、跨机一致。 |

## F3 热更方案（2026-06-13 拍板，暂记录、下一步再开工）

GitHub + Gitee 双公开仓库分发，安装程序自替换，**不依赖自建服务端**——可提前到 M5 之前独立做。

**形态**：启动查最新版本 → 有新版弹提示 + 更新日志（release body）→ 用户点更新 → 按竞速结果选源下载安装包 → 启动安装器 → Lumen 退出 → 安装器覆盖安装并重启。

**三项拍板（海风哥 2026-06-13）**：
1. **公开仓库**——GitHub（国外）+ Gitee（国内）两个公开仓库，零认证。
2. **选源 = 版本 API 竞速**——启动同时请求 GitHub 与 Gitee 的 `latest` Release API，谁先返回用谁那边下载（天然容错：一边被墙/挂了自动走另一边，免查 GeoIP）；可叠系统区域兜底。
3. **自替换 = 安装程序**——发版打包成 **Inno Setup** 安装包，覆盖安装时由安装器关闭运行中进程→替换→重启，Lumen 客户端不碰 exe 自替换。

**前置（开工前要有）**：①海风哥建 GitHub + Gitee 两个空公开仓库（给 owner/repo 名）②Inno Setup 打包脚本（我写）③发版流程脚本 tag→构建 release exe→打包→传两边 Release（我写）。

**客户端工作（我写）**：版本号取 `env!("CARGO_PKG_VERSION")` 对比 release tag；竞速查版本；下载安装包（带进度/校验）；拉起安装器 + 优雅退出。量级中等，不依赖 overlay 等其它未做项，随时可独立开工。

### ✅ F3 实现纪要（2026-06-13 夜间）

> **方案调整（2026-06-13 海风哥拍板，覆盖上文双源方案）**：**发布只走 GitHub，不发 Gitee**。仓库 = `jimhy/lumen`（`git@github.com:jimhy/lumen.git`；注意 owner 是 `jimhy` 非 `jimhyliu`）。已据此把 `update.rs` 改为**单 GitHub 源**（移除 `Source` 枚举/`Gitee`/竞速线程，`check_for_update` 单次 GitHub 请求；`GITHUB_REPO="jimhy/lumen"`）；更新弹窗版本行去掉「来自 X 源」改单参；`scripts/release.ps1` 去掉 Gitee 发布段；`Cargo.toml repository` 改 `jimhy/lumen`。**开源协议 = MIT**（已补根目录 `LICENSE` 文件 + README 许可章节，Cargo.toml 本就 `license="MIT"`，署名 `jimhy`）。下文「竞速两源/双仓库」描述为历史，以本调整为准。



- **HTTP 选型**：`ureq`（同步、自带 rustls，无需系统 OpenSSL）。HTTP+TLS 自实现成本高且无法离线 E2E 测，选成熟库最大化首次正确率。
- **`crates/lumen-app/src/update.rs`（新）**：`Version` 语义版本解析/比较（`v` 前缀、缺段补 0、pre-release 截断）；`parse_release_json`（兼容 GitHub / Gitee v5 的 `tag_name`/`body`/`assets[]`，优先 `.exe` 资产）；`check_for_update` 竞速两源（mpsc，首个成功源裁决，`CheckResult::{Newer,UpToDate,Failed}`）；`download_installer`（流式写 %TEMP% + 进度回调）；`launch_installer` / `installer_dest`（tag 清洗文件名）；`should_auto_check` 节流。**仓库 slug 常量 `GITHUB_REPO`/`GITEE_REPO` 当前 `jimhyliu/lumen`，海风哥建仓后核对**。8 单测（版本解析/比较/JSON 解析/节流/URL 等）。
- **设置**：`settings.rs` 加 `UpdateSettings { auto_check(默认 true) / skip_version / last_check_ms }`（serde default 向后兼容 + `from_value_lenient` 逐字段解析 + roundtrip 单测）。
- **i18n**：三语 16 条更新文案（toast/弹窗/按钮/设置）。
- **main.rs 接线**：启动 6h 节流自动检查（后台线程 + channel + `proxy.send_event(PtyWake)` 唤醒，与 PTY 唤醒同机制）；`user_event` drain 更新消息（发现新版→toast+弹窗；下载完成→拉起安装器→走 CloseRequested 同路径优雅退出）；run_ui 内联更新提示框（版本+日志+`立即更新`/`稍后`/`跳过此版本`）；设置→关于→更新（自动检查开关 + 检查更新按钮）。
- **发版产物**：`installer/lumen.iss`（Inno Setup：覆盖安装关进程重启、x64、图标随包）+ `scripts/release.ps1`（改版本号→release 构建→ISCC 打包→可选打 tag + `gh release create` + Gitee API 上传，附 SHA256）。
- **未做/取舍**：下载进度条 UI（当前进度回调为空实现，下载期间仅 toast）；SHA256 强校验（脚本产出 SHA 写入 release body，客户端暂未强制校验，依赖发布流程可信）；多源「版本更新者优先」（当前 race=首个返回者优先，两仓同步发版即一致）。
- **端到端验收前置**：海风哥建 GitHub+Gitee 公开仓库 + 发一个高版本 Release（带 `.exe` 资产）后方可真机验全链路（仓库不存在时检查静默失败，不影响使用）。

## F9 用户数据云同步（海风哥 2026-06-13 睡前提出，挂 M5 服务端之后）

> 登录账号后，个人数据上传服务器的**个人数据库**，**同账号多设备自动同步**。依赖 M5 服务端（登录体系 + 个人数据库），M5 落地后做。与 M5「远程控制」是两件事——这是**个人数据同步**，非远程操控。
> **海风哥点名**：M4.3 命令历史（history.jsonl）要能同步到其它同账号电脑。

**需同步数据清单（M5 时细化，初步梳理）**：

| 数据 | 来源 | 是否同步 | 备注 |
|---|---|---|---|
| **命令历史** | `history.jsonl` | ✅ 同步（海风哥点名） | 跨机共享；多设备来源按 `text+ts` 去重合并；cwd 字段跨机仅供展示/搜索过滤 |
| **UI 偏好**（主题 id / 语言 / 字号 / 经典模式开关 / 面板显隐 / Sync-with-OS 等） | `settings.json` 偏好字段 | ✅ 同步 | 用户偏好跨机一致 |
| **背景图设置** | `settings.json` background | ⚠️ 部分 | 开关/不透明度/暗化可同步；**本地图片路径不可同步**（别机失效）——要同步图本身需上传文件（后置） |
| **侧栏/文件树宽等布局像素** | `settings.json` layout | ⚠️ 斟酌 | 偏好性可同步，但屏幕尺寸不同体验未必一致 |
| **会话/窗格结构**（tab / 窗格 / cwd / 比例 / 最大化） | `sessions.json` | ⚠️ 容错同步或不同步 | cwd 路径机器相关（别机可能不存在）；同步价值弱，若同步须 cwd 失效回默认目录 |
| **窗口尺寸/位置/最大化态** | settings/sessions | ❌ 不同步 | 机器屏幕相关 |
| **自定义主题**（未来 P12 扩展用户自建主题） | — | ✅ 同步 | 用户造的主题跨机带走 |
| **自定义键位**（未来若做，设计 §4 后置项） | — | ✅ 同步 | |
| **补全/历史相关用户配置**（如启用项、忽略规则） | settings | ✅ 同步 | |
| **登录档案** | `profile.json` | — | 本就是账号系统一部分 |
| **运行态**（单实例 PID / shell integration 脚本 / 离屏纹理缓存 / 待解锁清单等） | — | ❌ 不同步 | 运行/机器态，无同步意义 |

**同步机制要点（M5 时设计）**：增量同步（按 `ts`/版本号 watermark）；多设备冲突合并（历史 `text+ts` 去重、偏好字段级 last-write-wins 或字段合并）；**离线优先**（本地为主、联网回灌，断网照常用）；登出后本地缓存清理策略待定；端到端可考虑加密（个人命令历史含敏感信息）。

## F10 终端可点击链接（海风哥 2026-06-13 提出，优先级待排）

> Lumen 终端/编辑器现**不支持点击打开链接**，现代终端标配（Warp/WT/iTerm 都有），需补。海风哥 2026-06-13 借「lumen.exe 路径点不开」点出此缺失；当前选先做 M4.4 sidecar，此项排队。
>
> **范围**：
> - **URL**（`http(s)://…`）click / ctrl+click → 开系统浏览器；
> - **文件路径**（`src/main.rs`、含 `:行:列` 如 `src/main.rs:10:5`）→ 开文件 / 定位到行；
> - **OSC 8 超链接**（程序显式发的可点击链接，如 `ls --hyperlink`、报错里的路径）采集；
> - hover 时下划线 + 手型光标提示可点。
>
> **涉及**：lumen-term（链接区间检测 / OSC 8 采集，从 PTY 流识别 URL/路径/OSC8）+ lumen-renderer（下划线渲染 + hover 态）+ lumen-app（鼠标命中链接区间 + ctrl/click 修饰键判定 + 打开：`open` crate 或 ShellExecute）。中等工程。

**✅ 代码完成（2026-06-13 夜间，待真机视觉/交互验收）**：
- **lumen-term**：`Cell` 加 `hyperlink_id: Option<NonZeroU32>`（NonZeroU32 借空指针优化，零额外字节）；`TermInner` 持 `hyperlinks: HashMap<id,URI>` + URI 反查去重 + 容量保护（4096 上限清表降级）；`osc_dispatch` 加 `b"8"` 分支 `handle_hyperlink`（URI 拼回/开启关闭语义/pen 落字归属）；公共 API `hyperlink_at` / `hyperlink_span_at`（本行内连续同 id 区段）。4 单测。
- **lumen-renderer**：`render`/`render_with_composer`/`render_impl` 加 `link_hover: Option<(abs行,起列,止列)>` 参数；逐 cell 循环命中区段画下划线（复用现有下划线管线，前景色）。
- **lumen-app `links.rs`（新）**：纯函数 `detect_link`（URL scheme 扫描 + 尾部标点/未配对括号平衡剥离；文件路径候选——含 `:行:列` 解析，盘符 `C:` 不误剥；`looks_like_path` 过滤裸词避免 hover 狂 stat）+ `resolve`（按 cwd 解析绝对路径 + 存在性校验，不存在不算链接）+ `open`（URL/文件走 explorer.exe；带行号且检测到 VS Code 走 `cmd /c code -g 路径:行:列`，缓存探测，回退默认程序）。13 单测。
- **lumen-app main.rs**：`hovered_link` 态 + `cell_under_mouse`/`link_at_mouse`/`update_link_hover`（单元格变化才重算，省行扫描/文件校验）；CursorMoved 非拖选时 hover 探测；Left Released 干净单击落链接 → 打开；run_ui 帧内 hover 时设手型光标（egui 拥有光标，帧尾 last-set）；渲染按窗格传 hover 区段。
- **已知限制/取舍**：①OSC 8 hover 跨行链接只取本行段（折行链接不跨行合并）②文件路径含空格须被引号包裹才识别（裸 token 按空白切分）③行号定位仅 VS Code（`code` CLI 在 PATH）；其它编辑器只「开文件」不定位行——可后续按需扩展。
- **验证**：clippy 0 warning；workspace 全测试绿（lumen-term 60 / lumen-app 373 等）；release 构建通过。

**海风哥验收反馈两轮 + 真机验证（2026-06-13，屏幕已解锁）**：
1. **打开改 Ctrl+Click**（反馈①「只 click 就开体验不好」）：普通单击保持选中/清除命令块，**Ctrl+单击**才打开（VSCode 终端惯例）。
2. **下划线长度/位置不对**（反馈②，截图实证）：根因=逐 cell 画下划线段，单元格宽非整数（9.375px）致亚像素接缝/错位。改为**整段一条连续 rect**（精确 `hs*cw`→`he*cw`，颜色取链接首格前景色贴合文字色）。
3. **手型光标始终显示 + hover 提示浮层**（反馈③，对标 Warp/VSCode）：hover 链接即显示手型；弹中文 tooltip「打开链接（Ctrl+单击）」/「打开文件（Ctrl+单击）」（egui Area，锚鼠标右下，不拦点击；i18n 三语）。
- **真机验证（截图 + 注入 + 日志，屏幕解锁后）**：①hover 输出行 URL → 截图证连续下划线精确覆盖 `http://127.0.0.1:18789` + tooltip「打开链接（Ctrl+单击）」浮出 ✓ ②普通 click → 无「打开链接」日志、浏览器进程数不变（不误开）✓ ③Ctrl+Click → 日志 `F10：Ctrl+Click 打开链接 Url(...)`、调起默认浏览器 ✓。
- **已知限制/取舍**：①OSC 8 hover 跨行链接只取本行段 ②文件路径含空格须被引号包裹才识别 ③行号定位仅 VS Code（`code` CLI 在 PATH），其它编辑器只「开文件」不定位行。

**验收反馈第三轮（2026-06-13，下划线偏位 + tooltip）**：
4. **手型光标始终显示 + hover tooltip**（已在第二轮做）：撤销 Ctrl-gate，hover 即手型；中文提示浮层「打开链接/文件（Ctrl+单击）」。
5. **下划线偏位根因修复（关键）**：海风哥 image#5 实证错误行（`http://...: The term 'http://...' is not recognized`）两个 URL 的下划线都偏位。**像素分析钉死**：下划线 rect 在网格列 34-56（x=833-1038，205px=22 列×9.375，**rect 位置完全正确**）；偏位的是**文字**——错误行是**粗体红字**，而 cosmic-text 渲染粗体时字形 advance ≠ 等宽 `cell_w`（系统缺 Cascadia Mono Bold→回退别的字体，advance 偏窄），长 ASCII 段内逐字累计漂移，文字整体偏离网格列，按网格列定位的下划线（及光标/选区，潜在）随之错位。普通文字 advance==cell_w 不漂（故「第一行 OK」）。**修复**（`lumen-renderer/lib.rs` 文本分段）：粗体/斜体 cell 改走**单字符段**（各自钉 `col*cw`，与 CJK/宽字符同款），消除段内漂移→文字回到网格列→下划线/光标/选区对齐；普通文字仍批量成段（性能不变）。真机截图验证：错误行引号 URL 下划线精确落在 `http://127.0.0.1:18789` 下。**取舍**：粗体字形若窄于 cell_w，逐字钉列后字间略有间隙（真等宽代价，正确性优先；普通文字不受影响）。clippy 0 warning、workspace 全测试绿、release 构建过。

## 待解锁验证清单（2026-06-12 凌晨，屏幕锁定期间积累，解锁后逐项补验）

> 锁屏（LogonUI）期间截图全黑、鼠标键盘注入失效（GetForegroundWindow=0）——凡视觉/交互验证均不可行，仅代码/日志/持久化层已验。**夜间自动化纪律：动手前先探测 LogonUI 进程，锁屏则降级为日志验证并入此清单。**

- [ ] F6：三语 UI 视觉目检（简中默认/繁体無豆腐块/英文排版），语言下拉真实点选→落盘→重启保持（代码层已验，缺视觉证据）
- [ ] M3.8：无边框窗口形态（阴影/圆角/1px 黑线）、标题栏拖动、双击最大化/还原、窗控三按钮、拖边 resize（winit #4186 风险点）、最大化不盖任务栏（8px 坑）、Snap Layouts 悬停弹出、IME 候选窗定位、NC 热区 egui hover 不亮（已知限制目检确认可接受度）
- [ ] M4.1：Compose 态打字/中文 IME（preedit 内嵌+候选框跟随）/单行与多行（Shift+Enter）提交/↑↓ 历史导航与放弃稿找回/退出码角标/Ctrl+C 三级语义+运行中中断（E9）/vim・codex 进出让位草稿保全/Fallback 降级（删 integration 脚本）/Ctrl+Shift+E 切换/footer 视觉（卡片/光标/状态条）/打字延迟不受输出影响（E1 体感）——验收标尺 E1/E2/E7/E8/E9
- [x] **F10 终端可点击链接**（2026-06-13 代码完成 + 真机验证）：✅ URL hover 连续下划线 + tooltip「打开链接（Ctrl+单击）」+ 手型、普通 click 不开、Ctrl+Click 开默认浏览器——均真机截图/注入/日志验证通过。**仍待海风哥目检的次要项**：文件路径单击开默认程序（带 `:行:列` 且装 VS Code 定位到行）、`ls --hyperlink` 等 OSC 8 真实程序链接、多窗格各自命中。
- [ ] **M4.2 OSC 633;C 权威命令文本对账**（2026-06-13 代码完成）：真机看 `LUMEN_DUMP_PTY` 或日志确认 `[BlockClosed] … cmd_text=Some(...)` 有值（integration.ps1 经 `-EncodedCommand` 注入，C 标记带 base64）；构造历史展开（`!!`）验证历史库以 shell 文本为准（代码层单测已验，缺真机字节流证据）。
- [ ] **F3 热更**（2026-06-13 代码完成，**单 GitHub 源**，需先发 Release 才能端到端验）：在 `jimhy/lumen` 发一个高版本 Release（带 `.exe` 安装包资产）后，启动 Lumen 应弹「发现新版本」提示框 + toast；点「立即更新」下载安装包→拉起安装器→Lumen 优雅退出；设置→关于→更新 的开关与「检查更新」按钮；「跳过此版本」持久化不再提示。

## 本批对抗性审查与修复（2026-06-13 夜间）

三功能落地后跑了一轮多 agent 对抗性代码审查（3 审查者分查 F10/M4.2/F3 + 逐条独立复核），确认 8 条真实 bug 并**全部修复**（非视觉项，纯逻辑/边界）：

| # | 功能 | 严重度 | 问题 | 修复 |
|---|---|---|---|---|
| 1 | M4.2 | **major** | 同批多块闭合时 `reconcile_text` 在 per-block 循环内改写 `entries[idx].text`，毒化同批后续 `backfill` 的匹配键 → 最后一条命令退出码/耗时丢失 | reconcile 移出循环，所有 backfill 后统一对账一次（取最后一个带 cmd_text 的块）；补回归单测 |
| 2 | F3 | **major** | 更新弹窗版本行模板用裸 `{}` 但走 `fmt2`（只认 `{0}/{1}`）→ 版本号/来源原样显示字面量、不替换 | 三语模板改 `{0}/{1}`；补三语插值回归单测 |
| 3 | F10 | minor | OSC 8 容量保护清表时 `next_hyperlink_id` 复位为 1 → 屏上旧链接 id 被新 URI 别名化（开错网址，非文档承诺的悬空→None） | 清表不复位 id（保持单调），旧 id 清表后查无→真正 None；同步修文档 |
| 4 | F10 | minor | 鼠标移出窗口（`CursorLeft`）后 `hovered_link` 不清，离屏纹理残留 hover 下划线 | 加 `WindowEvent::CursorLeft` 臂清 hover + 重绘 |
| 5 | F3 | minor | 竞速更新检查：先返回的滞后镜像源 `UpToDate` 短路裁决，掩盖另一源新版 | 仅 `Newer` 正向短路；`UpToDate` 不短路、等另一源 |
| 6 | F3 | minor | 竞速 `recv_timeout` 每轮各重置 10s 窗口，最坏可 ~2× | 改统一单调截止时刻（`Instant`），两轮合计 ≤ 一个窗口 |
| 7 | F3 | minor | `download_installer` 无读取超时，慢响应/僵死使下载线程永久挂起 + `update_downloading` 永留 true | 加 `timeout_read(60s)` + 出错清半截文件 |

修后：clippy 0 warning；workspace 全测试绿（lumen-app 372→新增 2 回归测试）；release 构建通过；启动冒烟存活无 panic。

## 新会话接力指引

下一步队列（2026-06-13 夜间更新）：**M4 输入编辑器 M4.1–M4.4 全部完成并真机验收通过**；**本批（海风哥授权「除远程外其它所有功能全做」）已完成 F10 可点击链接 / M4.2 OSC 对账 / F3 热更（代码层，待真机验收，详见各小节与待解锁清单）**。**剩余未做功能（均属「远程」，本批未做）**：①**F9 用户数据云同步**（挂 M5 服务端，含完整同步数据清单，见「F9」小节）②**M5 远程控制**（下一大里程碑：状态同步 + 操作指令协议，服务端 lumen-server）。下一步走哪个待海风哥拍板。

**⚠️ 本批发现的既有缺陷（非本批引入，待海风哥定夺是否修）**：`cargo build -p lumen-app --no-default-features`（剔除 input-editor feature 的经典构建）**早已编译不过**——`main.rs` 三处（历史搜索 `history_rows_owned`、`completion_candidates`）在非 feature-gated 上下文引用 input-editor 专属字段（M4.3/M4.4 引入时未回归该构建，与本批 F10/F3 无关，本批改动在此配置下零新增错误）。默认构建（即发布构建，input-editor 开启）一切正常。若要恢复「flag 剔除双验证」纪律需把这三处 cfg-gate。

工程纪律：构建后必须强制重编核对（此机 cargo 指纹陷阱）；引擎级修复必须真机注入复现验证；agent 验证日志不入库。
